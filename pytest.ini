[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
markers =
    golden_sandbox: optional local sandbox pass-rate baseline (AO-P0)
filterwarnings =
    ignore::DeprecationWarning
